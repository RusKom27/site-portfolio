// import styles from './page.module.css';

import React from "react";

async function Home() {
    return (
        <main>
            {/*{posts.map((x, i) => <div key={i}>{ `${ x }` }</div>)}*/}
        </main>
    );
}

export default Home;